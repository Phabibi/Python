# Assingment 4.1
# First last repeated
# Parsa Habibi
def first_last_repeated(a,b):
    total=str(a[0]*int(b))+" "+str(a[len(a)-1]*b)
    return total
print (first_last_repeated("abcd",4))    
